# prime_checker is a pure-stdlib CLI/library -- it does not read any
# environment variables or secrets at runtime. This file is kept for
# consistency with the portfolio-wide scaffold template; there is
# currently nothing to copy into a real .env.
