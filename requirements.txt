# Core package (src/prime_checker) has no runtime dependencies -- pure stdlib.

# Test suite
pytest==8.3.3

# Only needed by the legacy exploratory scripts at the repo root
# ("Prime Number Best Method.py", "primenumbers.py"), not by src/ or tests/.
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
